[package]
name = "locardx-operation-manager"
version.workspace = true
edition.workspace = true
description = "LocardX job queue, scheduling, progress tracking, and cancellation"

[dependencies]
locardx-common = { path = "../common" }
