[package]
name = "locardx-drive-eraser"
version.workspace = true
edition.workspace = true
description = "LocardX whole-disk sanitization and overwrite engine"

[dependencies]
locardx-common = { path = "../common" }
